@page "/categories/edit/{Id:int}"


<h3>Editar País</h3>
@* no puedo tirar el formulario de una porque el country es null se puede demorar un segundo por eso hago la pregunta *@
@if(category is null)
{
    <Loading/>
}
else
{
    <FormWihtName @ref="categoryForm" Label="Pais" Model="category" OnValidSubmit="EditAsync" ReturnAction="Return" />

@*     <CategoryForm @ref="categoryForm" Category="category" OnValidSubmit="EditAsync" ReturnAction="Return" />
 *@}






 